{
  "name": "cf-mcp-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Generic MCP OAuth proxy Worker for Cloudflare Access",
  "scripts": {
    "postinstall": "patch-package",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "cf-typegen": "wrangler types",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@cloudflare/workers-oauth-provider": "^0.2.3",
    "just-pick": "^4.2.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^25.2.3",
    "patch-package": "^8.0.0",
    "typescript": "5.9.3",
    "wrangler": "^4.65.0"
  }
}